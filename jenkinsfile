pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
           git branch: 'man', credentialsId: 'c32bcd78-8e80-4992-aec2-47abf50f1179', url: 'https://github.com/Teja-Aws/Sample.git'
                  }
        }
       stage('Changing User/Group and Permission') {
            steps {
               
                sh 'chmod -R 777 /var/lib/jenkins/workspace/App'
                sh 'chown -R jenkins:jenkins /var/www/html/'
            }
        }
        stage('Deploy') { 
            steps {
                sh 'cp -R * /var/www/html/'
            }
        }
    }
}
